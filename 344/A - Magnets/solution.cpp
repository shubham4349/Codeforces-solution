#include<bits/stdc++.h>using namespace std; int main(){    int n; cin>>n;        vector<string> magnet;    while(n--){        string s;        cin>>s;        magnet.push_back(s);    }    int total=1;    string prev=magnet[0];   // int cnt=0;    for(int i=1; i<magnet.size(); i++){                if(prev!=magnet[i]){            total++;         }         prev=magnet[i];    }    cout<<total;}